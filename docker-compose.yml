version: '3'
services:
  web:
    image: "nginx:alpine"
    ports:
     - "80:80"
    volumes: 
     - ./nginx.vh.default.conf:/etc/nginx/conf.d/default.conf:ro
  app:
    build: .
    ports:
     - "5000:5000"
  database:
    image: "postgres:alpine"
